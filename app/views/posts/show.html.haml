.row
  .span2
    %b
      = @post.title
  .span10
    = raw @post.content
.row 
  .span2
    = "author: #{@post.author}"
  .span10
    = "tags: #{@post.tag_list}"

- @post.images.each do |image_field|
  = link_to image_tag(image_field.image.url(:thumb)), image_field.image.url(:original), :rel => "prettyPhoto"
- if @editable
  = link_to "Edit", edit_post_path(@post)
  = link_to "Delete", post_path(@post), :method => :delete
= render :template => "/comments/new"
= render :template => "/comments/index"

